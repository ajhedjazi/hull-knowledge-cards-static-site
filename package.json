{
  "name": "hull-knowledge-cards",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/publish-static.mjs",
    "preview": "vite preview",
    "generate:routes": "node scripts/generate-route-options.mjs",
    "verify:routes": "node scripts/verify-route-options.mjs",
    "verify:routes:live": "node scripts/verify-route-options.mjs --live",
    "test:speech": "node scripts/test-route-speech-score.mjs"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.6.0",
    "vite": "^7.0.4"
  }
}
